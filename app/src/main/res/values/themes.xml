<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DevList" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <Item name="colorPrimary">@color/my_light_primary</Item> -->
        <item name="android:statusBarColor">@color/purple</item>
    </style>

    <style name="Theme.DevList" parent="Base.Theme.DevList" />

    <style name="NoAnimationBottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="android:background">?android:attr/windowBackground</item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.ActionBar</item>
    </style>

</resources>